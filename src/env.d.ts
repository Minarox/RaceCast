/// <reference types="astro/client" />

/**
 * Environment variables are declared in astro.config.mjs (`env.schema`) and
 * reached through `astro:env/server` / `astro:env/client`, which generates its
 * own types — nothing to declare here.
 */
